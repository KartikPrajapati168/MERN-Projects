// src/utils/mongodb.js
console.log('🔄 Initializing MongoDB Connection from Frontend...');

const connectDB = () => {
  console.log('✅ MongoDB Connected Successfully (Simulated for Review 1)');
  console.log('📦 Database: WasteExchangeCluster (LocalStorage Mode)');
  console.log('🗂️  Collections: users, listings, requirements, deals');
  console.log('💡 Note: Backend APIs will be integrated in Phase 2 (after Review 1)');
};

connectDB();

export default connectDB;